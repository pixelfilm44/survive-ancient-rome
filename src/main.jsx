import React from "react";
import ReactDOM from "react-dom/client";
import SurviveRome from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <SurviveRome />
  </React.StrictMode>
);
